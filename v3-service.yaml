#v3-service.yaml:
apiVersion: v1
kind: Service
metadata:
  name: chat-v3-service    #any-name-service
spec:
  type: NodePort
  selector:
    app: chat-v3-app     #imagename
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30014
